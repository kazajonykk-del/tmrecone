export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});try{const d=req.body||{};if(!d.name||!d.total)return res.status(400).json({error:'Invalid data'});const token=process.env.TELEGRAM_BOT_TOKEN,chat=process.env.TELEGRAM_CHAT_ID;if(!token||!chat)return res.status(500).json({error:'Telegram env is not configured'});const text=`🧠 IT QUIZ PRO — YANGI NATIJA\n\n👤 F.I.Sh: ${d.name}\n📚 Bo‘lim: ${d.mode}\n📊 Natija: ${d.correct}/${d.total} (${d.pct}%)\n✅ To‘g‘ri: ${d.correct}\n❌ Xato: ${d.wrong}\n⏱ Test yakunlandi.`;const tg=await fetch(`https://api.telegram.org/bot${token}/sendMessage`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({chat_id:chat,text})});const out=await tg.json();if(!out.ok)return res.status(502).json({error:'Telegram rejected request'});return res.status(200).json({ok:true})}catch(e){return res.status(500).json({error:'Server error'})}}
